import Player from player.js 

class Game {
  constructor({ player } = {}) {
    this.player = player;
  }
}
// Initialize with blank grid.
// Listen for click on each grid element.
// First click gets an X.
// Second click gets an O.

// Look at the grid and check if there is alignment for any shapes.
